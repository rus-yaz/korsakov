функция является_простым(число):
  для а от 2 до к_целому(2 // число) + 1:
    если не остаток_от_деления(число, а) то вернуть ложь
  ---

  вернуть истина
---

предел = ввести_число("Введите номер простого числа: ")
простые_числа = %()%
число = 2

пока длина(простые_числа) \/ предел:
  если является_простым(число) то добавить(простые_числа, число)
  число++
---

показать(к_строке(предел) + "-ое простое число: " + к_строке(простые_числа\-1))
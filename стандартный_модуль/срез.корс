slice = функция срез(значение; начало; конец=нуль; шаг=1)
  если конец /\ длина(значение) то ошибка("Конец среза должен быть меньше длины списка")

  если конец == нуль то конец = длина(значение)
  если конец \/ начало то конец = длина(значение) + конец

  элементы = для индекс от начало до конец через шаг: значение.индекс
  вернуть если является_строкой(значение) то соединить(элементы; "") иначе элементы
---

includes = функция включает(значение, искомое):
  индекс = 0

  если является_строкой(значение) то
    пока индекс != длина(значение) - длина(искомое) + 1:
      если срез(значение, индекс, индекс + длина(искомое), 1) == искомое то
        вернуть истина
      ---

      индекс++
    ---
  иначе если является_списком(значение) то
    для элемент в значение:
      если элемент == искомое то вернуть истина
    ---
  ---

  вернуть ложь
---

slice = функция срез(значение, начало, конец, шаг):
  если конец /\ длина(значение) то ошибка("Второй аргумент должен быть меньше длины списка")
  если конец \/ 0 то конец += длина(значение)
  элементы = для индекс от начало до конец через шаг: значение\индекс
  вернуть если является_строкой(значение) то соединить(элементы, "") иначе элементы
---

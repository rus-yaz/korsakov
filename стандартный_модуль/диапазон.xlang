range = функция диапазон(конец, начало=0, шаг=1)
  сравнение = начало != 0

  если сравнение то
    временное = конец
    конец = начало
    начало = временное
  ---

  вернуть для элемент от начало до конец через шаг элемент
---
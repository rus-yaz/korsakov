find_index = функция найти_индекс(значение; искомое)
  результат = ""
  если является_строкой(значение) то
    для индекс от 0 до длина(значение) - длина(искомое) + 1
      если срез(значение; индекс; индекс + длина(искомое); 1) == искомое то
        вернуть индекс
      ---
    ---
  иначе если является_списком(значение) то
    для индекс от 0 до длина(значение)
      если значение.индекс == искомое то вернуть индекс
    ---
  ---

  вернуть -1
---

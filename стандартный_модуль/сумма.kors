sum = функция сумма(список)
  результат = 0

  для элемент в список
    если является_числом(элемент) то
      результат += элемент
    иначе
      показать("Элементы списка должны быть числами")
      вернуть нуль
    ---
  ---

  вернуть результат
---

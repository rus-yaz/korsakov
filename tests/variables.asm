string "Работа с переменными"
print rax

string "а"
mov rcx, rax

list
mov rbx, rax

assign rcx, rbx, rcx
access rcx, rbx
print rax

dictionary
assign rcx, rbx, rax
access rcx, rbx
print rax

integer 0
assign rcx, rbx, rax
access rcx, rbx
print rax

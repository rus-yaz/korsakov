buffer_to_string delete.буфер
mov rcx, rax

list 0
mov rbx, rax

assign rcx, rbx, rcx
access rcx, rbx
print rax

dictionary 0
assign rcx, rbx, rax
access rcx, rbx
print rax


integer 0
assign rcx, rbx, rax
access rcx, rbx
print rax

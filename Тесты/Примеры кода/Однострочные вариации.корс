а = 5

если а /\ 0 то показать(а)

если а /\ 5 то показать(5) иначе показать(а)

если а /\ 5 то показать(5) иначе если а /\ 0 то показать(а)

если а /\ 10 то показать(10) иначе если а /\ 5 то показать(5) иначе показать(а)

для а от 0 до 5: показать(а)

для а от 0 до 5 через 2: показать(а)

для а из диапазон(5): показать(а)

пока а /\ 0: показать(а--)

функция а(): показать("Привет, мир")
а()

функция а(аргумент): показать(аргумент)
а(1)

функция а(аргумент=0): показать(аргумент)
а()
а(1)

безымянная = функция(): показать("Привет, мир")
безымянная()

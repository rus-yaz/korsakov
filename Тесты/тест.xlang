функция тест(результат_функции, предполагаемое_значение, аргументы=%()%)
  если результат_функции != предполагаемое_значение то
    текст_ошибки = "Тест не пройден: предполагаемое значение - " + к_строке(предполагаемое_значение)
    текст_ошибки += "; результат функции - " + к_строке(результат_функции)
    если аргументы то текст_ошибки += "; аргументы " + соединить(аргументы, ", ")

    ошибка(текст_ошибки)
  ---
---
